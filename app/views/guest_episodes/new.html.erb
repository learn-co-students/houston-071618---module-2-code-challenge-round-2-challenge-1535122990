<%= form_with model: GuestEpisode.new do |form|  %>
  <%= form.select :episode_id, Episode.all.map {|p| [p.number, p.id]} %>
  <%= form.select :guest_id, Guest.all.map {|p| [p.name, p.id]} %>
  <%= form.number_field :rating, placeholder: "Rating" %>
  <%= form.submit %>
<% end  %>
